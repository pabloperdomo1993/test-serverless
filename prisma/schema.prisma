generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-1.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://root:123456789@testdatabase.cnkwxeoangrz.us-east-2.rds.amazonaws.com:5432/testDatabase?schema=public"
}

model User {
  id    Int     @default(autoincrement()) @id
  number  String @unique
  enumerationType String
  name String
  countryCode String
  city String
  state String
  taxonomyDesc String
  nameData Json?  @db.Json
}